# Eval set

This folder will hold the labeled evaluation set (~30 queries with relevance
labels) and scoring scripts. It is populated during M4 per PROJECT_SPEC.md §28.

M0 status: empty placeholder — nothing to evaluate yet.